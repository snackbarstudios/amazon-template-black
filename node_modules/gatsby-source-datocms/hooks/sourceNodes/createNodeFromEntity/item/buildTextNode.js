"use strict";

var buildNode = require('../utils/buildNode');

var _require = require('humps'),
    pascalize = _require.pascalize;

module.exports = function buildTextNode(itemNode, key, text, mediaType) {
  var str = text || ' ';
  return buildNode('DatoCmsTextNode', "".concat(itemNode.id, "-").concat(key), function (node) {
    node.internal.mediaType = mediaType;
    node.internal.content = str;
  });
};